<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools" android:id="@+id/activity_show_attendance_record" android:layout_width="match_parent" android:layout_height="match_parent" android:orientation="vertical" android:padding="16dp" tools:context=".ShowAttendanceRecord">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="سجل الحضور"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:paddingBottom="16dp" />

    <!-- اختيار تاريخ البداية -->
    <TextView
        android:id="@+id/startDateText1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Start Date"
        android:textSize="16sp"
        android:padding="12dp"
        android:background="#EEEEEE"
        android:drawableEnd="@android:drawable/ic_menu_my_calendar"
        android:gravity="start|center_vertical" />

    <!-- اختيار تاريخ النهاية -->
    <TextView
        android:id="@+id/endDateText1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="End Date"
        android:textSize="16sp"
        android:padding="12dp"
        android:background="#EEEEEE"
        android:drawableEnd="@android:drawable/ic_menu_my_calendar"
        android:gravity="start|center_vertical"
        android:layout_marginTop="8dp" />

    <!-- زر البحث -->
    <Button
        android:id="@+id/showAttendanceBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="عرض السجلات"
        android:layout_marginTop="16dp"
        android:backgroundTint="@color/primaryColor"
        android:textColor="@android:color/white" />

    <!-- قائمة السجلات -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/attendanceRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_weight="1"
        android:scrollbars="vertical" />

</LinearLayout>
